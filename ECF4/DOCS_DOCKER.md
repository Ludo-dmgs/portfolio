# Déploiement / Dockerisation - Portfolio (ECF4)

## Prérequis
- Docker Desktop (Windows/Mac) ou Docker Engine (Linux)
- Port 8080 (ou 8081) disponible.

## Lancer en local depuis les sources
```bash
docker compose up --build
# http://localhost:8080
